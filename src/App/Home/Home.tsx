import React from 'react';
import { Card } from '@material-ui/core';

const Home = () => (
    <div>
        <Card elevation={0}>
            <p>Home</p>
        </Card>
    </div>
);

export default Home;
